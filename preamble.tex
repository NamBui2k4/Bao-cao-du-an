\usepackage{tabularx}
\usepackage{plantuml}
\usepackage{lipsum}  % For dummy text
\usepackage{float}
\usepackage{makecell}
\usepackage{booktabs}
\usepackage{pifont}
\usepackage{enumitem}
\usepackage{tabularray}
\usepackage{scrextend}
\changefontsizes{13pt} % Ép toàn bộ nội dung về 13pt chuẩn Word
% \usepackage{newtxtext,newtxmath} % Font Times New Roman chuẩn và đẹp nhất cho LaTeX

% ----- Encoding & Font -----
\usepackage{polyglossia}
\usepackage{fontspec}
\setmonofont{Courier New}
\setmainfont{Times New Roman}
\usepackage{unicode-math} 
\setmathfont{Cambria Math}

% --- Colors (bắt buộc trước listings) ---
\usepackage[dvipsnames,table]{xcolor}
% dvipsnames: thêm nhiều tên màu có sẵn; 'table' để màu trong bảng booktabs nếu cần.

% ----- Vietnamese? -----
\setmainlanguage{english}
% \setotherlanguage{english}

\addto\captionsvietnamese{%
  \renewcommand{\bibname}{Bibliography}
}

% ----- Margin -----
\usepackage[
  a4paper,
  top=3.5cm,
  bottom=3cm,
  left=3.5cm,
  right=2cm
]{geometry}

% ----- Line spacing ~1.5 -----
\usepackage{setspace}
\onehalfspacing

% ----- Graphics -----
\usepackage{graphicx}  % Chèn hình
\graphicspath{{images/}}
% \usepackage{url}


% ----- Caption formatting -----
\usepackage[font=small]{caption}


% ----- Table packages -----
\usepackage{array}
\usepackage{booktabs}
\usepackage{multirow}

% ----- Math -----
\usepackage{amsmath}


% ----- Code -----
\usepackage{listings}
\lstdefinestyle{tree}{
  language=bash,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{green!50!black},
  showstringspaces=false,
  columns=fixed,            % 🔥 giữ độ rộng ký tự cố định
  keepspaces=true,
  frame=single,
  rulecolor=\color{black!30},
  xleftmargin=10pt,
  xrightmargin=10pt,
  aboveskip=1em,
  belowskip=1em
}
\lstset{
  basicstyle=\ttfamily\normalsize,
  frame=none,
  breaklines=true,
  breakatwhitespace=true,
  columns=fixed,
  keepspaces=true,
  captionpos=b,
  xleftmargin=0.5cm,
  xrightmargin=0.5cm
}
\lstdefinestyle{codeblock}{
  basicstyle=\ttfamily\small,
  columns=fixed,
  keepspaces=true,
  showstringspaces=false,
  frame=single,
  rulecolor=\color{black!30},
  xleftmargin=10pt,
  aboveskip=1em,
  belowskip=1em
}
\lstdefinelanguage{YAML}{
  keywords={true,false,null,y,n},
  keywordstyle=\color{blue}\bfseries,
  sensitive=false,
  comment=[l]{\#},
  commentstyle=\color{gray}\ttfamily,
  stringstyle=\color{green!50!black}\ttfamily,
  morestring=[b]",
  morestring=[b]',
}
\lstdefinelanguage{Docker}{
  keywords={FROM, RUN, CMD, LABEL, EXPOSE, ENV, ADD, COPY, ENTRYPOINT, VOLUME, USER, WORKDIR, ARG},
  keywordstyle=\color{blue}\bfseries,
  sensitive=false,
  comment=[l]{\#},
  morecomment=[s]{/*}{*/},
  commentstyle=\color{gray}\ttfamily,
  stringstyle=\color{green!50!black}\ttfamily,
  morestring=[b]",
}

\lstdefinelanguage{JavaScript}{
  keywords={break, case, catch, class, const, continue, debugger, default,
    delete, do, else, export, extends, finally, for, function, if, import,
    in, instanceof, let, new, return, super, switch, this, throw, try,
    typeof, var, void, while, with, yield, async, await},
  keywordstyle=\color{blue}\bfseries,
  ndkeywords={true,false,null,undefined},
  ndkeywordstyle=\color{magenta},
  identifierstyle=\color{black},
  sensitive=true,
  comment=[l]{//},
  morecomment=[s]{/*}{*/},
  commentstyle=\color{gray}\ttfamily,
  stringstyle=\color{red}\ttfamily,
  morestring=[b]',
  morestring=[b]"
}


% ----- Bibliography -----
\usepackage{cite}
\usepackage[hidelinks]{hyperref}

% ----- Paragraph indentation (1 tab ≈ 1.27 cm) -----
\setlength{\parindent}{1.27cm}

% ----- Page numbering at top center -----
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

